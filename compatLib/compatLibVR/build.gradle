plugins {
    alias(libs.plugins.android.library)
}

android {
    namespace "app.mica.compatlib.eleven"
}

addFrameworkJar('framework-11.jar')

dependencies {
    api projects.compatLib.compatLibVQ
}
