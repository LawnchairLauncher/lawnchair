plugins {
    alias(libs.plugins.android.library)
}

android {
    namespace "app.mica.compatlib.twelve"
}

addFrameworkJar('framework-12.jar')

dependencies {
    api projects.compatLib.compatLibVR
}
